// Código generado automáticamente de test/test2.py
// Traducción de Python a JavaScript

let contador_externo = 0;
let limite_externo = 4;
let limite_interno = 6;
let acumulador = 0.0;
let estado = true;
while (contador_externo < limite_externo) {
	if(estado) {
	if(acumulador == 0.0) {
	for (let indice_interno = 0; indice_interno < limite_interno; indice_interno++) {
	acumulador = 1.5 + acumulador;
}
estado = false;
let  mensaje[] = "El acumulador ha sido actualizado";
}
contador_externo = 1 + acumulador;
}
acumulador}

